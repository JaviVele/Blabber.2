{
    "scripts": {
        "ng": "ng",
        "start": "npx concurrently \"cd /backend && npx sails lift\" \"npm start\" \"npm build\"",
        "build": "npx concurrently \"cd /frontend && npx build\" \"npm start\""
    }
}